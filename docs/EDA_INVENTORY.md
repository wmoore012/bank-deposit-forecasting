# Exploratory evidence retained in both notebook editions

The concrete-teaching revision preserves all twelve EDA chart identifiers from the
pre-rewrite inventory: reporting_banks_time, median_deposits_time, next_report,
decline_share_time, training_seasonality, cash_by_outcome_box, feature_correlations,
persistence_eda, target_growth_side_by_side, small_denominator_growth, missingness,
and coverage_time. The regression suite checks each identifier and reading guide.

The source preview, column dictionary, types, `info()`, missing counts, `describe()`,
data chips, all five numeric micro-profiles with box/violin distributions, and
full-data quartile receipts remain visible. Descriptive calculations use the
stated source/audit or training population; outcome relationships are labelled.
Display windows never remove valid observations from model training or scoring.

The masterclass additionally retains seed, bootstrap, uninsured-field,
foundation-model, and archived Experiment 0 diagnostics. No EDA topic was removed
to shorten a social-media story. Exports select evidence from the notebook.
