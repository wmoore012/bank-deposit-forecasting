# Experiment 0: dollar-runoff formulation

This directory preserves the earlier executed masterclass and exact score outputs.
`Assign1.ipynb` remains at the project root and is unchanged. The original notebook
expects the project root as its working directory so it can resolve `data/` and
`vendor/`; its outputs use the old `masterclass_outputs` directory.

The revised notebooks reproduce 94.5443558% concentration using the original
USD 10 million minimum, consecutive prior/next reports, training-size quartiles,
and 2024 Q1–Q3 predictor rows. This is different from the new regression population.
The archived network capture is an equal-quarter average at 10% capacity;
the concentration statistic pools decline dollars across the period.
